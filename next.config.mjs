/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aceternity.com'], // Add the domain for the images you're using
      },
};


export default nextConfig;

